# NOTE: intentionally includes older dependencies for dependency-scanning tests.
Flask==1.0.2       # older Flask release
requests==2.18.4   # older requests with known issues
PyYAML>=5.1        # Update PyYAML to safe version 5.1+ to avoid arbitrary code execution
boto3==1.7.0       # older boto3 version